<h1>New order</h1>
  <div class="depot_form"
       <fieldset>
         <legend>Please enter your details</legend>
         <% render 'form' %>
       </fieldset>
</div>
<%= render 'form' %>

<%= link_to 'Back', orders_path %>
